package com.qa.util;

import com.qa.factory.DriverFactory;
import com.qa.uat.pages.Approvalpages;
import com.qa.uat.pages.LoginPage;
import com.qa.uat.pages.paymentpage;

public interface BaseTest {
	
	//Analyticspage ap= new Analyticspage();
	
	

//	WaitFunctions w = new WaitFunctions();
	//LoginPage logintest = new LoginPage(DriverFactory.getDriver());
//	Leadsanddealspage Leaddealspage = new Leadsanddealspage(DriverFactory.getDriver());
	//Leadprofilepage lp = new Leadprofilepage(DriverFactory.getDriver());
	
	//mailloginpage mail = new mailloginpage(DriverFactory.getDriver());
	//Dealspage dealspage = new Dealspage(DriverFactory.getDriver());
	//AssertionMethods assertions = new AssertionMethods(DriverFactory.getDriver());
	
//	paymentpage payments=new paymentpage(DriverFactory.getDriver());
	
//	Approvalpage approvalpage= new Approvalpage(DriverFactory.getDriver());
	//LoginPage login=new LoginPage(DriverFactory.getDriver());
//	Screenshotpage screenshotpage= new Screenshotpage();

}
